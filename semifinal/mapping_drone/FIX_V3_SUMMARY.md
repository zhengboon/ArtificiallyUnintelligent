# Fix Workflow Output Summary

## mapping_drone/controller.py
- size: 45173 bytes
- fix_notes: Three surgical fixes applied to mapping_drone/controller.py and nothing else. (A) The mock-mode UWB mirroring inside _telemetry_loop is verified wired correctly: after updating state.drone_down from pvn.position.down_m, we check isinstance(self.uwb, MockUwbNode) and isinstance(self.drone, MockMavsdk), then call self.uwb.set_position(pvn.position.north_m, pvn.position.east_m) BEFORE the subsequent uwb.get_position() read so the very next line picks up the fresh fix (this is the fix the v2 patch claimed but apparently did not deliver effectively). MockMavsdk._snapshot_pvn already yields SimpleNamespace(position=SimpleNamespace(north_m, east_m, down_m), velocity=...) matching real MAVSDK shape, and _tick already integrates north_m/east_m/down_m from velocity. (B) The --runs-dir default was changed from "semifinal/mapping_drone/runs" to "mapping_drone/runs" so invoking from inside semifinal/ no longer produces semifinal/semifinal/... nested paths; help text updated to reflect the relative-to-CWD convention. (C) In _register_sighting, the save_marker_image call now passes bbox_xyxy=bbox so reviewers see the green rectangle and id label burned into the saved frame. No other classes, signatures, CLI flags, mock behavior, watchdogs, or existing fixes were modified.

## mapping_drone/run_writer.py
- size: 20426 bytes
- fix_notes: Applied two surgical fixes to mapping_drone/run_writer.py. (A) In add_sighting, replaced the crashing `pad_id = rec["aruco_id"]` with a defensive `pad_id = rec.get("aruco_id")` placed BEFORE appending to self._sightings, with an early-return + `logger.warning("skipping malformed sighting: missing aruco_id")` when None — this prevents the KeyError regression introduced when _sighting_to_dict began omitting (rather than fabricating) required fields. The malformed sighting is also not appended to self._sightings, which avoids polluting run_summary.json with half-records. (B) In _pad_for_serialization, dropped the dead-code legacy fallbacks: 'bbox' now reads pad['bbox'] only (no 'bbox_xyxy' fallback) and 'validity_classification' is read directly (no 'valid' fallback / ternary). Updated the docstring to note canonical-only behaviour and added the requested in-line comment `# canonical schema keys: aruco_id, world_xyz_m, pixel_center, bbox, image_path, validity_classification` above the self._pads[pad_id] = {...} assignment in add_sighting. Atomic .tmp+os.replace writes, the RLock, save_marker_image bbox overlay, _sighting_to_dict body, write_status, finalise, and _atomic_write_text are all untouched.

## Re-audit