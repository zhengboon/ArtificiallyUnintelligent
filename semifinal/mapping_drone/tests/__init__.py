"""Smoke tests for the mapping_drone package.

These are pure standalone scripts — pytest is NOT a dependency. Each script
can be run via ``python -m mapping_drone.tests.<name>`` and prints a final
``PASS`` or ``FAIL`` line so an operator can interpret the result by eye.
"""
