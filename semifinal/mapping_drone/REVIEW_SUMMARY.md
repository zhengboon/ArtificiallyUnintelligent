# Workflow Output Summary

Total files: 8

## mapping_drone/__init__.py
- code size: 925 bytes
- impl notes: Standard package marker that re-exports the public surface from sibling submodules (uwb, realsense, mapping, validity, run_writer). Importing the package will eagerly import all submodules; if any submodule has a heavy optional dependency (rclpy, pyrealsense2, rknnlite) that's missing, those submodules should handle their own graceful degradation internally — this __init__ does no try/except guarding so import failures surface loudly during integration.
- verdicts: ['pass_with_minor_issues', 'pass', 'pass_with_minor_issues']
- total issues raised: 4

## mapping_drone/uwb.py
- code size: 6637 bytes
- impl notes: Depends on rclpy + geometry_msgs at runtime for UwbNode only; module import never fails on machines without ROS2 because the import is wrapped in try/except. MockUwbNode is always available. UwbNode owns its rclpy.init() only if it was the one to call it, so it cleanly coexists with other ROS2 nodes (e.g., a future ROS2-based MAVSDK alternative) in the same process. Spin runs in a daemon thread at 100ms granularity. The ENU->NED swap (n=pose.y, e=pose.x) matches kolomee.py exactly. The integrator should call start() before get_position() and stop() in a finally block.
- verdicts: ['pass_with_minor_issues', 'pass_with_minor_issues', 'pass_with_minor_issues']
- total issues raised: 13

## mapping_drone/realsense.py
- code size: 8550 bytes
- impl notes: RealsenseNode depends on pyrealsense2 (librealsense); import is lazy and degrades gracefully so MockRealsenseNode + the deproject helper work on laptops without it. Mock requires opencv-contrib for cv2.aruco; it handles both modern (generateImageMarker) and older (drawMarker) APIs. Mock intrinsics use SimpleNamespace with rs.intrinsics field names (ppx/ppy, not cx/cy) so the deproject helper's getattr path works uniformly. Depth is uint16 millimetres in both real and mock; integrators must divide by 1000 before passing to deproject_pixel_to_camera_xyz, which expects metres.
- verdicts: ['pass_with_minor_issues', 'pass_with_minor_issues', 'pass_with_minor_issues']
- total issues raised: 16

## mapping_drone/mapping.py
- code size: 15516 bytes
- impl notes: cv2 (opencv-contrib-python for aruco) and numpy are required at import time; pyrealsense2 is imported lazily inside _deproject only. The OccupancyGrid uses a subsample step of 4 px to keep integrate() cheap on the RK3588 — adjust _INTEGRATE_PIXEL_STEP if more density is needed. Grid is centered on origin_offset_m (default 0,0) and spans size_m on each side; landing pads outside that window are silently clipped from render. camera_to_world assumes the gimbal rotates the camera about its X-right axis (pitch=-90 -> straight down) and that drone yaw is degrees clockwise-from-north (standard MAVSDK convention).
- verdicts: ['needs_fixes', 'pass', 'needs_fixes']
- total issues raised: 15
- CRITICAL/HIGH issues:
  - [CRITICAL] camera_to_world() and OccupancyGrid.integrate() — z_b formula: Sign error in the gimbal-pitch -> body-up component. Formula is `z_b = -sp*z_c - cp*y_c`. Trace with the canonical mapping case (gimbal_pitch=-90, camera looking straight down) and a floor point at cam_xyz=(0,0,depth=2): sp=-1, cp=0 -> z_b = -(-1)*2 - 0 = +2. This gives u_off=+2, i.e. u_world = drone_alt + 2 (point ABOVE drone), but a floor point seen by a down-facing camera at 2 m depth is 2 m BELOW the drone, i.e. should be drone_alt - 2. Result: every landing-pad world_xyz_m altitude and the entire OccupancyGrid height_sum are inverted in sign as soon as the gimbal is tilted. Pitch=0 happens to work (z_b = -y_c), which masks the bug in any forward-looking unit test.
    -> fix: Change to `z_b = sp * z_c - cp * y_c` in BOTH camera_to_world() and OccupancyGrid.integrate(). Verify with: pitch=0,(0,1,5) -> z_b=-1 (below, ok); pitch=-90,(0,0,2) -> z_b=-2 (below, ok); pitch=-90,(0,1,0) -> z_b=0.
  - [CRITICAL] camera_to_world() and OccupancyGrid.integrate() — pitch rotation block: The pitch rotation is wrong-signed and will invert the world-Z (up) of every depth point. At gimbal_pitch_deg=-90 (straight down, the primary mapping mode), a point at camera (0,0,depth) lands directly on the ground (body z = -depth, world up offset = -depth, i.e. below the drone). The code computes x_b = cp*z_c - sp*y_c and z_b = -sp*z_c - cp*y_c, which at pitch=-90 (cp=0, sp=-1) gives x_b=+y_c (wrong sign) and z_b=+z_c (wrong sign — places ground points ABOVE the drone). Result: occupancy grid integration and any ArUco world_xyz both put pads/ground above the drone instead of below. The grid will be empty/garbage in the downward-facing case, and landing-pad world_xyz_m will be nonsense.
    -> fix: Use x_b = cp*z_c + sp*y_c and z_b = sp*z_c - cp*y_c. Verify pitch=0 still gives (x_b=z_c, y_b=x_c, z_b=-y_c) and pitch=-90 gives (x_b=-y_c, y_b=x_c, z_b=-z_c). Apply identical fix in OccupancyGrid.integrate().
  - [HIGH] camera_to_world() and OccupancyGrid.integrate() — x_b formula (likely): Probable companion sign error in the forward component. `x_b = cp*z_c - sp*y_c`. At gimbal_pitch=-90, sp=-1, so a camera point with +y_c (image-down direction) contributes +y_c to body-forward. But when the camera is rotated nose-down 90°, the camera +Y axis (originally pointing toward image bottom = body-down at pitch=0) ends up pointing REARWARD in body frame, i.e. -X_body. So the contribution should be `-y_c`, not `+y_c`. This means objects toward the bottom of the image are reported as in FRONT of the drone instead of BEHIND it. Less obvious than the z_b bug but flows from the same rotation-matrix sign mistake.
    -> fix: After re-deriving R_pitch consistently, the pair should be `x_b = cp*z_c + sp*y_c` and `z_b = sp*z_c - cp*y_c` (verify against pitch=0 baseline: x_b=z_c, z_b=-y_c — both still match the working forward-looking case). Add a unit test that asserts a (0,0,depth) point at pitch=-90 lands at (drone_n, drone_e, drone_alt-depth).

## mapping_drone/validity.py
- code size: 3232 bytes
- impl notes: Pure-stdlib module — no external deps. Env var MAPPING_DRONE_VALIDITY is read fresh on every call (not cached) so judges/operators can swap rules mid-run if needed via process env, though typical usage is set-once-at-startup. When org publishes the real rule, replace the lambda for `_RULES['even']` (or add a new entry and change `_DEFAULT_RULE`).
- verdicts: ['pass_with_minor_issues', 'pass_with_minor_issues', 'pass_with_minor_issues']
- total issues raised: 9

## mapping_drone/run_writer.py
- code size: 12307 bytes
- impl notes: Depends on cv2 (opencv-python) and numpy; both are already required by realsense.py/mapping.py so no new dependency. ArucoSighting is duck-typed via getattr/dict-get so a circular import with mapping.py is avoided — any object exposing the contract's attribute names works. landing_pads.json is rewritten on every add_sighting() so the file stays current if the run is killed mid-flight; this is fine at expected sighting rates (<100 over a run) but if sightings are integrated thousands of times per second the controller should throttle. STATUS.txt and run_summary.json use a .tmp + os.replace pattern so judges never read a half-written file. The thread-safety contract uses a single RLock and assumes the caller doesn't mutate sighting objects after handing them off.
- verdicts: ['needs_fixes', 'pass_with_minor_issues', 'pass_with_minor_issues']
- total issues raised: 21
- CRITICAL/HIGH issues:
  - [HIGH] RunWriter.add_sighting (replacement branch): When a new sighting replaces an existing pad (because the prev had no world_xyz, or new has >= confidence), the replacement record loses history: first_seen_at is set to the NEW sighting's timestamp (overwriting the true earliest sighting), and sighting_count resets to 1 (discarding prior count). Only the else branch correctly maintains last_seen_at and increments count.
    -> fix: On replacement, carry over prev's first_seen_at and sighting_count: first_seen_at = prev['first_seen_at'] if prev else rec['first_seen_at']; sighting_count = (prev['sighting_count'] + 1) if prev else 1; last_seen_at = rec['first_seen_at'].
  - [HIGH] RunWriter.finalise / _write_pads_locked — landing_pads.json schema: Project spec for landing_pads.json requires fields {aruco_id, world_xyz_m, pixel_center, bbox, image_path, validity_classification}. The code emits 'bbox_xyxy' instead of 'bbox' and 'valid' instead of 'validity_classification'. Judges/graders consuming the documented schema will not find the expected keys.
    -> fix: In the pad record stored in self._pads, rename 'bbox_xyxy' -> 'bbox' and 'valid' -> 'validity_classification' (or alias them at serialization time in _write_pads_locked).

## mapping_drone/controller.py
- code size: 31244 bytes
- impl notes: Depends on sibling modules (.uwb, .realsense, .mapping, .validity, .run_writer) implementing the contract — must be run as a package (python3 -m mapping_drone.controller), not as a script. Real-MAVSDK path imports `mavsdk` lazily so `--mock-all` works without it installed. Real-UWB path requires `rclpy`; `--mock-uwb` bypasses that. Signal handlers are best-effort on Windows (loop.add_signal_handler unsupported there). The status writer task and telemetry pumps are cancelled cleanly on shutdown. Position-stuck detection only arms after STUCK_GRACE_S from mission start. fly_to_position_velocity has its own 30s per-waypoint timeout layered on top of --max-flight-time-s. The hover-mode `5.0` multiplier on the proportional gain during transit (vs. KP_XY alone for hold) is a deliberate amplification so the controller actually traverses 2m hops in reasonable time — original kolomee.py was for short-range station-keeping; tune at venue if needed.
- verdicts: ['needs_fixes', 'needs_fixes', 'pass_with_minor_issues']
- total issues raised: 32
- CRITICAL/HIGH issues:
  - [HIGH] FlightState.drone_yaw / _telemetry_loop: drone_yaw is declared in FlightState and passed to OccupancyGrid.integrate() and camera_to_world(), but is NEVER updated from telemetry. position_velocity_ned does not contain yaw. World-frame coordinates therefore always assume yaw=0, which is incorrect for any non-trivial flight where the drone rotates.
    -> fix: Subscribe to drone.telemetry.attitude_euler() (or attitude_quaternion()) in an additional background task and update self.state.drone_yaw from the yaw_deg field. Mirror in MockMavsdk._MockTelemetry.
  - [HIGH] _check_safety position-stuck watchdog + run_mission: started_at is set at the very beginning of run_mission (before AWAITING_UWB, ARMING, TAKEOFF). STUCK_GRACE_S=10s easily elapses before takeoff completes. Because the drone is stationary on the ground during this entire pre-flight sequence, _check_safety will report 'position-stuck' as soon as fly_to_position_velocity begins, aborting the mission immediately.
    -> fix: Either start the stuck clock only after takeoff (e.g. set a separate self.state.airborne_at when in_air first becomes True and use that for stuck grace), or short-circuit _check_safety while self.state.in_air is False.
  - [HIGH] _build_uwb (mock path) interaction with _check_safety stuck-watchdog: In --mock-all / --mock-uwb runs, MockUwbNode is initialised at (0,0) and never updated, so state.drone_n/drone_e remain 0 for the entire flight. Combined with issue above, position_history is flat and the stuck-watchdog will abort every mock run after STUCK_WINDOW_S (~20s).
    -> fix: Either drive MockUwbNode.set_position from the mock drone's position (e.g. in a background task that copies MockMavsdk._pos into the mock UWB), or skip the stuck-watchdog when the active UWB adapter is a MockUwbNode.
  - [HIGH] run() — main lifecycle, no try/finally around drone arm/takeoff: If run_mission() raises before reaching the landing block (e.g. during arm/takeoff/offboard prewarm), the drone may be left armed and airborne. The mission_task exception path does call emergency_land(), but only when stop_task did NOT win the race. However, if controller.run_mission() raises an exception that is NOT caught inside, the await mission_task in the else branch catches it and calls emergency_land — OK. But if the drone is armed and taking off and the user presses Ctrl-C, mission_task.cancel() will raise CancelledError inside run_mission, which currently has NO try/finally to land/disarm. The cancellation propagates out, then emergency_land is called separately — that path works, but run_mission itself has zero try/finally protection. Any exception inside run_mission between arm and land leaves armed-airborne state with no guaranteed cleanup inside that function.
    -> fix: Wrap the body of run_mission() in try/finally that always attempts offboard.stop, action.land, and action.disarm. Do not rely solely on the outer run() to call emergency_land — run_mission owns the armed state.
  - [HIGH] MappingController.run_mission — safety abort during mission does not land: When fly_to_position_velocity returns False due to abort (battery low, UWB lost, stuck), the code does `if not reached and self.state.aborted: break` and falls through to the normal land sequence. That land sequence calls drone.action.land() which is fine, BUT the abort condition is UWB-loss — meaning we have NO position fix. Continuing to the normal land block (which still tries offboard.stop + action.land) is correct, but there is no guarantee land() succeeds without GPS. There is no fallback to emergency_land() or kill on land failure; the code only logs `land failed` and continues to disarm-while-airborne, which would drop the drone.
    -> fix: On safety abort, call emergency_land() explicitly and do not blindly disarm after a failed land. Gate disarm on `not self.state.in_air` rather than just sleeping 15s and disarming unconditionally.
  - [HIGH] MappingController.run_mission — disarm-while-airborne risk: After action.land(), the loop waits up to 15s (30 * 0.5s) for self.state.in_air to be False, then UNCONDITIONALLY calls action.disarm(). If land takes longer than 15s, or if the in_air telemetry stream stalled, disarm will be sent while still airborne, causing a crash. Same pattern exists in emergency_land().
    -> fix: Only disarm if `not self.state.in_air` is confirmed; otherwise log and keep waiting (or extend timeout substantially). Never disarm on a timeout fallback.
  - [HIGH] MappingController._check_safety — UWB-loss watchdog never fires before first fix: The watchdog only triggers if `self.state.last_uwb_ts > 0`. If UWB never produced a fix (e.g. mock not set, or ROS topic silent), last_uwb_ts stays 0.0 and the watchdog is permanently disabled, yet the mission proceeds (the AWAITING_UWB block only warns and continues). The drone would fly with stale/zero position and the stuck-watchdog is the only fallback — but stuck-watchdog uses the same drone_n/drone_e that never updated, so position_history would show 'stuck at 0,0' and might catch it, OR it might not if drone_n/e were updated by some other path. Net effect: UWB-loss-from-the-start is silently tolerated for 10s of flight.
    -> fix: If UWB never becomes ready and --real, refuse to arm. At minimum, treat last_uwb_ts == 0 after takeoff as 'UWB never seen' and abort after UWB_LOSS_LAND_S from mission start.
  - [HIGH] MappingController.run_mission — health check breaks on first sample regardless of is_armable: `async for h in self.drone.telemetry.health(): if getattr(h, 'is_armable', True): break  else await asyncio.sleep(0.2)`. The `await asyncio.sleep(0.2)` is inside the else of the if, but the loop just `break`s on the first sample if is_armable is True OR missing. If is_armable is False, the loop sleeps then iterates again — but `async for` only yields when the stream produces, and many health streams emit only on change. This could hang indefinitely waiting for is_armable to become True with no timeout. Also `getattr(h, 'is_armable', True)` defaults to True if attribute missing, silently bypassing the check.
    -> fix: Add a wall-clock timeout (e.g. 15s); abort if not armable. Do not default missing is_armable to True — default to False or raise.

## mapping_drone/README.md
- code size: 8423 bytes
- impl notes: Pure documentation file — no runtime dependencies. References the interface contract from the project description (UwbAdapter, RealsenseAdapter, ArucoDetector, OccupancyGrid, RunWriter, decide_landing_validity, controller.main) so it stays consistent with the implementer agents' outputs. Mentions org reference scripts by name (kolomee.py, generateTopDown.py, getDepthAndDetect.py, rknndecoder.py) so the reader can cross-check our adaptation against the originals. The --detector flag is described as a future hook, not as currently implemented — controller.py does not need to ship it for Challenge 1.
- verdicts: ['pass_with_minor_issues', 'pass_with_minor_issues', 'pass_with_minor_issues']
- total issues raised: 20
- CRITICAL/HIGH issues:
  - [HIGH] Down vs Up altitude convention in flow + contract: README flow step 1 says drone pose uses PX4 NED 'down_m' (negative airborne). Contract's OccupancyGrid.integrate also takes down_m. But contract's camera_to_world takes 'drone_alt_m' which by name implies Up (positive airborne, per project's 'world frame U=Up'). README does not flag this sign-flip footgun, so an implementer will likely pass down_m straight into camera_to_world and produce world Z mirrored about the floor.
    -> fix: Add an explicit note in the README architecture/flow section: 'Convert PX4 down_m to up_m (alt = -down_m) before calling camera_to_world; OccupancyGrid.integrate keeps the raw down_m.' Better: align the contract so both helpers take the same sign.
  - [HIGH] --gimbal-pitch default -45 vs documented coordinate convention: Project context states the camera-frame convention (Z=Forward, X=Right, Y=Down) only holds 'when the camera faces DOWN' (i.e., gimbal -90). README defaults --gimbal-pitch to -45. At -45 the documented axis identities are wrong, so any camera_to_world implementation that assumes the documented convention will produce incorrect world coords by default. Mapping a top-down occupancy grid at -45 also distorts the grid integration.
    -> fix: Default --gimbal-pitch to -90 (the documented mapping config), and clearly state that other angles require full 3-axis rotation rather than the simplified down-camera shortcut.

## Cross-module audit
- verdict: needs_fixes
- summary: The package will RUN under `python3 -m mapping_drone.controller --mock-all` (no crashes, completes mission, writes all artifacts) but produces degenerate output because the Mock UWB never advances, so the drone perceives itself stuck at (0,0) and every recorded landing-pad world coord clusters at the origin. For `--real` there are two blocking bugs that will hit on first telemetry tick / first yawed flight: (1) `_telemetry_loop` reads `pvn.down_m` directly, but real MAVSDK PositionVelocityNed exposes `.position.down_m` — AttributeError will kill pose feedback and the safety watchdogs; (2) `drone_yaw` is initialised to 0 and never updated because nothing subscribes to `attitude_euler()` — with the default `--gimbal-pitch=-45` every fused world_xyz for the landing pads (the core Challenge 1 deliverable) will be rotated by whatever the actual yaw happens to be. Several smaller integration gaps: marker images are saved without the bbox overlay both the contract and README promise; Windows Ctrl-C bypasses emergency_land + finalise entirely; the UWB 1-second hold-position grace is defined as a constant but never enforced; STATUS.txt and landing_pads.json compute pad validity from different sources; describe_rule() and a configurable ArUco dict are documented/exported but unwired. None of the contract method signatures are broken, and the file layout matches the contract — the issues are all behavioural/integration. Fix the two critical items (PVN.position.down_m + attitude_euler subscription) and the high-severity mock-UWB feedback before any venue use; the rest are polish that should land before flight day but won't prevent a run.
- issues: 14
- [CRITICAL] controller.py :: MappingController._telemetry_loop: Uses `pvn.down_m` directly, but real MAVSDK's PositionVelocityNed yields a struct with `.position.down_m` and `.velocity.down_m_s` (see learning_material_3_uwb/kolomee.py line 116 which uses `pos.position.down_m`). The mock _MockPosVelNed has a flat `down_m` attribute so mock-all happens to work, but the real --real run will crash with AttributeError on the very first telemetry tick, killing pose feedback and the safety/fly_to loops. This is the single biggest blocker for venue use.
  -> fix: Change to `self.state.drone_down = float(pvn.position.down_m)` and update _MockPosVelNed/MockMavsdk so its telemetry stream yields an object with the same nested .position.down_m shape (or wrap the mock value in a SimpleNamespace before yielding).
- [CRITICAL] controller.py :: FlightState.drone_yaw / MappingController._telemetry_loop: drone_yaw is initialised to 0.0 and never updated anywhere in the controller. Nothing subscribes to `drone.telemetry.attitude_euler()` (kolomee.py does this via attitude_task). With the default --gimbal-pitch=-45 the camera->world transform is highly yaw-sensitive, so EVERY recorded landing-pad world_xyz_m (and every depth point in OccupancyGrid.integrate) will be wrong by an arbitrary rotation as soon as the drone yaws. Challenge 1 deliverable is per-pad world coords — this directly corrupts the deliverable.
  -> fix: Add an `_attitude_loop` task mirroring kolomee.attitude_task that does `async for a in drone.telemetry.attitude_euler(): self.state.drone_yaw = float(a.yaw_deg)`; start it alongside the other telemetry tasks; extend MockMavsdk._MockTelemetry with an `attitude_euler()` async generator yielding a stub with `yaw_deg`.
- [HIGH] controller.py :: --mock-all integration with MockUwbNode: _build_uwb sets the mock UWB to (0,0) once and nothing in the controller ever advances it. Mock MAVSDK does integrate velocity into self._pos.north_m/east_m, but the controller reads N/E exclusively from UWB (only D comes from PVN), so state.drone_n/_e stay at 0.0 forever in mock-all. Result: fly_to_position_velocity for any non-origin waypoint never converges, eats the full 30s timeout per waypoint, and every recorded sighting ends up clustered at world (0,0,U). End-to-end 'completes' but produces a degenerate map — not a useful smoke test of the mapping pipeline.
  -> fix: Either (a) have the mock MAVSDK push its integrated NED position into the MockUwbNode each tick (controller can hold a reference and call uwb.set_position(self.drone._pos.north_m, self.drone._pos.east_m) inside _telemetry_loop when isinstance(uwb, MockUwbNode)), or (b) in mock-all, source N/E from PVN.position instead of UWB.
- [HIGH] run_writer.py :: RunWriter.save_marker_image: Writes the raw frame.color_bgr unchanged. Both the contract (`save bbox-overlaid color frames of each sighting`) and the README ('markers/marker_<id>_<seq>.jpg bbox-overlaid frames of each sighting') promise bbox-annotated images. Judges/reviewers opening markers/*.jpg will see plain frames with no visible indication of which marker was detected.
  -> fix: Have the controller draw the bbox + ArUco id with cv2.rectangle/cv2.putText on a frame copy before calling save_marker_image, or extend save_marker_image to accept bbox_xyxy + aruco_id and do the drawing inside RunWriter.
- [HIGH] controller.py :: _build_drone (real path): After connecting, only waits for `drone.core.connection_state().is_connected`. Does NOT wait for `health.is_local_position_ok` (or is_global) the way kolomee.py does before arming. PX4 will refuse the arm in offboard if local position isn't ok, which translates to a confusing failure at the venue. The run_mission() does loop on `health.is_armable`, but is_armable does not imply position estimate — and after the arm the offboard pre-warm+start will silently reject if position isn't locked.
  -> fix: In _build_drone (real path), after is_connected, also `async for h in drone.telemetry.health(): if h.is_local_position_ok: break` with a sane timeout. Log clearly when local position becomes OK.
- [MEDIUM] controller.py :: fly_to_position_velocity, hover branch: When the drone reaches the waypoint, the function sends ONE hover-velocity setpoint then returns. The mission loop then enters the 'scan' phase that does send 0,0,0 velocity at 10 Hz, which is fine, but during the inter-iteration period of fly_to itself there is no hover hold — every call returns immediately on arrival without continuing to correct drift. Combined with the gentle KP=0.1 this is acceptable but loses the kolomee hover() behaviour the contract appears to expect.
  -> fix: Either keep the loop alive for a short settle window (e.g. hold for 0.5s of hover-band corrections before returning True), or extract a hover_for(seconds) helper used both at the waypoint and during scanning.
- [MEDIUM] controller.py :: run() Ctrl-C path on Windows: add_signal_handler is swallowed on Windows so Ctrl-C falls through to KeyboardInterrupt in main(). main() catches it and returns 130 WITHOUT ever calling controller.emergency_land() or run_writer.finalise(). On a real flight launched from the Windows C2 Terminal that means a Ctrl-C will skip the land+disarm path and skip writing top_down.{png,npy}/run_summary.json — exactly the opposite of the 'Ctrl-C -> emergency_land coroutine' safety requirement.
  -> fix: On Windows, install a `signal.signal(SIGINT, _on_signal)` fallback that sets stop_requested via loop.call_soon_threadsafe (or run a tiny watcher task that polls a threading.Event). Alternatively wrap the asyncio.run call in try/except KeyboardInterrupt that performs a best-effort synchronous land+finalise.
- [MEDIUM] controller.py :: run_mission UWB watchdog vs hold-position spec: Contract specifies 'UWB-loss watchdog: hold position 1s on loss, land if sustained >5s'. _check_safety only enforces the 5s land-if-lost branch; the 1s hold-position behaviour (UWB_LOSS_GRACE_S) is defined as a constant but never used. During brief UWB dropouts the fly_to controller will simply act on stale n/e values from FlightState instead of explicitly holding, which can produce a small drift commit.
  -> fix: In fly_to_position_velocity (and ideally in a higher-level guard), if (now - last_uwb_ts) > UWB_LOSS_GRACE_S and <= UWB_LOSS_LAND_S, force vn=ve=0 and only allow vd corrections, until UWB returns or land triggers.
- [MEDIUM] controller.py :: _unique_pads vs run_writer pad validity: Two divergent sources of truth for per-pad validity: _unique_pads (used for STATUS.txt) recomputes via decide_landing_validity(aid) every status tick, while RunWriter._pads stores the validity captured at first-sighting time (used for landing_pads.json and run_summary.json). If MAPPING_DRONE_VALIDITY env var ever changes mid-run, or if a future rule depends on external state, the live STATUS shown to judges will disagree with the persisted JSON.
  -> fix: Pick one canonical source. Easiest: cache validity per aruco_id in MappingController at first sighting (dict[int,bool]) and use the same dict for both STATUS snapshot building and run_writer.add_sighting.
- [MEDIUM] controller.py :: _load_waypoints DEFAULT_WAYPOINTS: DEFAULT_WAYPOINTS has 5 entries (the square plus a return-to-home), but the README/contract both describe it as a '4-waypoint demo square'. Cosmetic, but if anyone trusts the docs and counts waypoints they will be confused; the off-by-one also affects the rough timing budget under --max-flight-time-s.
  -> fix: Either trim DEFAULT_WAYPOINTS to 4 (drop the trailing (0,0,1.5)) or update README to say 'square plus return-to-origin (5 waypoints)'.
- [LOW] validity.py :: describe_rule: describe_rule() is exported and clearly intended to be logged at startup ('Intended for the controller to log at startup, e.g. logger.info(...)'), but controller.py never imports or calls it. Operators reading the log have no record of which validity rule was in effect for a given run.
  -> fix: In controller.run() after _setup_logging, add `from .validity import describe_rule; logger.info('validity rule: %s', describe_rule())` and include the rule name in run_summary.json via run_writer.finalise.
- [LOW] mapping.py :: top-of-file imports: `import json` is imported but never used in mapping.py. Minor dead code.
  -> fix: Remove the unused import.
- [LOW] run_writer.py :: _sighting_to_dict bbox_xyxy default: Default for missing bbox_xyxy is (0,0,0,0); for missing pixel_center is (0,0). These will silently appear as 'detection at origin with zero area' if a malformed sighting is ever recorded, masking the real bug. Per the contract bbox/pixel_center are required.
  -> fix: If the sighting lacks these fields, raise or log a warning rather than substituting a plausible-looking zero tuple.
- [LOW] controller.py :: run() — no log of detector dict / no support for --aruco-dict: Contract bullet says 'Configurable via constant' for the ArUco dict, but controller.py hard-codes ArucoDetector() with default '6X6_250' and exposes no CLI flag. If org changes the dict on the day there is no runtime override; you must edit code.
  -> fix: Add `--aruco-dict 6X6_250` argparse flag and pass to ArucoDetector(dict_name=args.aruco_dict). Log the chosen dict at startup.