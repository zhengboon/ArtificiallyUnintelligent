title: ArtificiallyUnintelligent
description: BrainHack 2026 RoboVerse Drone Challenge — University Finals, Marina Bay Sands, 10–11 June 2026.
# theme intentionally omitted — we ship our own _layouts/default.html + assets/css/style.css.

# Pretty URLs
permalink: pretty

# Markdown
markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false
  syntax_highlighter: rouge
  toc_levels: 2..3

plugins:
  - jekyll-relative-links
  - jekyll-seo-tag
  - jekyll-sitemap

relative_links:
  enabled: true
  collections: true

include:
  - CONCEPT_PLAN.md

exclude:
  - LINKEDIN_POST_DRAFT.md
  - images/README.md
  - vendor
  - Gemfile
  - Gemfile.lock

url: "https://zhengboon.github.io"
baseurl: "/ArtificiallyUnintelligent"
github:
  repository_url: https://github.com/zhengboon/ArtificiallyUnintelligent

defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: default
